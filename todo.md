1. rename the "ai receiptionist", "receiptionmist" to "agent", also the supabase table name
2. yung url is /{current_page}/{current_agent_id}
3. in dashboard page, when creating new agent, the modal will show like in the image i attached
4. in dashboard page, agents lists should be 3 items per row only 
5. remove clinics, it should be organization, every user have one organization that can create multiple agents
6. add phone numbers page, and add tables in supabase
7. after signin, it should be redirect to /dashboard/{first_agent_id}
8. in landing page, if the there's authenticated use "Logout" and "Dashboard" instead of "Signin" and "Create an account
9. create new page "outbound message"
10. create new page "analytics"
11. remove the "Logout" and "aaronaludo@gmail.com Receptionist online · 2.1s avg response" at sidebar